#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

typedef struct {
	int start;
	int end;
} Interval;

int main(){

	// int* a = (int*)malloc(5 * sizeof(int));
	// memset(a, -1, 5 * sizeof(int));
	// for(int i = 0; i < 5; i++)
	// 	printf("%d\n", a[i]);

	printf("%d\n", 1 << 20);
	printf("I LOVE CODING VERY MUCH!!!!\n\n");
	return 0;
}